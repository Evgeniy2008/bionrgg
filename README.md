# Bionrgg - Цифрові візитки нового покоління

Сучасна платформа для створення цифрових візиток з підтримкою всіх популярних соціальних мереж.

## 🚀 Особливості

- **Сучасний дизайн** - Мінімалістичний та елегантний інтерфейс
- **Швидкість** - Оптимізований код без лагів
- **Мобільна адаптивність** - Ідеально працює на всіх пристроях
- **Безпека** - Захищені API та валідація даних
- **SEO-оптимізація** - Мета-теги та структурована розмітка

## 🛠 Технології

- **Frontend**: HTML5, CSS3, Vanilla JavaScript
- **Backend**: PHP 7.4+
- **База даних**: MySQL
- **Стилі**: CSS Grid, Flexbox, CSS Variables
- **Анімації**: CSS Animations, Intersection Observer

## 📁 Структура проекту

```
bionrgg-main/
├── index.html              # Головна сторінка
├── create.html             # Створення профілю
├── login.html              # Авторизація
├── profiles.html           # Таблиця лідерів
├── profile.html            # Перегляд профілю
├── my-profile.html         # Управління профілем
├── assets/
│   ├── css/
│   │   ├── main.css        # Основні стилі
│   │   ├── animations.css  # Анімації
│   │   ├── forms.css       # Стилі форм
│   │   ├── profiles.css    # Стилі профілів
│   │   ├── profile.css     # Стилі профілю
│   │   └── my-profile.css  # Стилі управління
│   ├── js/
│   │   ├── main.js         # Основний функціонал
│   │   ├── forms.js        # Обробка форм
│   │   ├── profiles.js     # Лідери
│   │   ├── profile.js      # Перегляд профілю
│   │   └── my-profile.js   # Управління профілем
│   └── img/                # Зображення
├── api/
│   ├── leaderboard.php     # API лідерів
│   ├── register.php        # API реєстрації
│   ├── login.php           # API авторизації
│   ├── get-profile.php     # API отримання профілю
│   ├── track-view.php      # API відстеження переглядів
│   ├── update-profile.php  # API оновлення профілю
│   └── delete-profile.php # API видалення профілю
├── .htaccess               # Налаштування сервера
└── README.md               # Документація
```

## 🗄 База даних

### Таблиця `users`
```sql
CREATE TABLE `users` (
  `username` varchar(50) NOT NULL,
  `password` varchar(255) NOT NULL,
  PRIMARY KEY (`username`)
);
```

### Таблиця `users_info`
```sql
CREATE TABLE `users_info` (
  `username` varchar(50) NOT NULL,
  `descr` text,
  `inst` varchar(255) DEFAULT '',
  `discord` varchar(255) DEFAULT '',
  `fb` varchar(255) DEFAULT '',
  `steam` varchar(255) DEFAULT '',
  `twitch` varchar(255) DEFAULT '',
  `tiktok` varchar(255) DEFAULT '',
  `tg` varchar(255) DEFAULT '',
  `youtube` varchar(255) DEFAULT '',
  `linkedin` varchar(255) DEFAULT '',
  `github` varchar(255) DEFAULT '',
  `x` varchar(255) DEFAULT '',
  `whatsapp` varchar(255) DEFAULT '',
  `reddit` varchar(255) DEFAULT '',
  `site` varchar(255) DEFAULT '',
  `djinni` varchar(255) DEFAULT '',
  `dou` varchar(255) DEFAULT '',
  `olx` varchar(255) DEFAULT '',
  `amazon` varchar(255) DEFAULT '',
  `prom` varchar(255) DEFAULT '',
  `binance` varchar(255) DEFAULT '',
  `fhunt` varchar(255) DEFAULT '',
  `upwork` varchar(255) DEFAULT '',
  `fiverr` varchar(255) DEFAULT '',
  `views` int(11) DEFAULT 0,
  `avatar` longtext,
  `bg` longtext,
  `color` varchar(7) DEFAULT '#c27eef',
  `colorText` varchar(7) DEFAULT '#ffffff',
  PRIMARY KEY (`username`)
);
```

## ⚙ Налаштування

1. **Створіть базу даних MySQL**:
   ```sql
   CREATE DATABASE bionrgg;
   ```

2. **Налаштуйте підключення до БД** в файлах API:
   ```php
   $connectDB = new mysqli("localhost", "root", "root", "bionrgg");
   ```

3. **Створіть таблиці** використовуючи SQL з розділу "База даних"

4. **Налаштуйте веб-сервер** (Apache/Nginx) з підтримкою PHP

## 🎨 Дизайн

### Кольорова схема
- **Основний**: #c27eef (фіолетовий)
- **Вторинний**: #6366f1 (синій)
- **Акцент**: #f59e0b (жовтий)
- **Фон**: #0a0a0a (чорний)
- **Текст**: #ffffff (білий)

### Шрифти
- **Основний**: Inter (Google Fonts)
- **Резервні**: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto

## 📱 Підтримувані соцмережі

- Instagram
- Discord
- Facebook
- Steam
- Twitch
- TikTok
- Telegram
- YouTube
- LinkedIn
- GitHub
- X (Twitter)
- WhatsApp
- Reddit
- Особистий сайт
- Djinni
- DOU
- OLX
- Amazon
- Prom.ua
- Binance
- Freelancehunt
- Upwork
- Fiverr

## 🚀 Функціонал

### Для користувачів
- ✅ Створення профілю за 1 хвилину
- ✅ Налаштування дизайну (кольори, аватар, фон)
- ✅ Додавання посилань на соцмережі
- ✅ Перегляд статистики
- ✅ Копіювання посилання на профіль
- ✅ Видалення профілю

### Для відвідувачів
- ✅ Пошук профілів
- ✅ Перегляд профілів
- ✅ Таблиця лідерів
- ✅ Адаптивний дизайн

## 🔧 API Endpoints

| Endpoint | Method | Опис |
|----------|--------|------|
| `/api/leaderboard.php` | GET | Отримати топ-5 профілів |
| `/api/register.php` | POST | Реєстрація нового користувача |
| `/api/login.php` | POST | Авторизація |
| `/api/get-profile.php` | GET | Отримати дані профілю |
| `/api/track-view.php` | POST | Відстежити перегляд |
| `/api/update-profile.php` | POST | Оновити профіль |
| `/api/delete-profile.php` | POST | Видалити профіль |

## 📈 Оптимізація

- **CSS**: Мінімізація, змінні, Grid/Flexbox
- **JavaScript**: Модульність, події, асинхронність
- **PHP**: Підготовлені запити, транзакції
- **Зображення**: Base64 кодування, оптимізація
- **Кешування**: HTTP заголовки, стиснення

## 🛡 Безпека

- Валідація всіх вхідних даних
- Підготовлені SQL запити
- Захист від XSS та CSRF
- Безпечні заголовки HTTP
- Валідація файлів

## 📞 Підтримка

Якщо у вас виникли питання або проблеми, створіть issue в репозиторії.

---

**Bionrgg** - Об'єднуйте всі свої соцмережі в одному місці! 🚀